# Core
numpy>=1.24.0
pandas>=2.0.0
pydantic>=2.0.0

# LLM (CPU version - works everywhere)
# For GPU acceleration, run: python scripts/install_llama_gpu.py
llama-cpp-python>=0.2.0
openai>=1.0.0
python-dotenv>=1.0.0
sentence-transformers

# Retrieval
rank-bm25>=0.2.2
# Dense retrieval (torch is installed manually to match the cluster CUDA version)
faiss-cpu>=1.9
sentence-transformers>=5.0

# Data
datasets>=2.14.0
huggingface-hub>=0.17.0

# Evaluation
scikit-learn>=1.3.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0
tqdm>=4.65.0

kagglehub>=0.1.0
graphrag

# LightRAG graph retrieval (HKUDS) + local model serving for Qwen.
# vllm 0.7.3 pins torch==2.5.1 (matches the existing env); newer vllm forces
# torch>=2.6 and would break the BM25/dense pipeline — do not bump unbounded.
# transformers must stay <5: vllm 0.7.3 uses tokenizer.all_special_tokens_extended,
# which transformers 5.x removed (crashes vLLM at tokenizer init). 4.57.x satisfies
# vllm (>=4.48.2) and sentence-transformers (>=4.41,<6).
lightrag-hku>=1.5.3
vllm==0.7.3
transformers>=4.48.2,<5